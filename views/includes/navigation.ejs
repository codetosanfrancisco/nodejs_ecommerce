<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li
        class="<%= path === '/' ? ['nav-item','active'].join(' ') : 'nav-item' %>"
      >
        <a class="nav-link" href="#">Home </a>
      </li>
      <li
        class="<%= path === '/products' ? ['nav-item','active'].join(' ') : 'nav-item' %>"
      >
        <a class="nav-link" href="/products">Products</a>
      </li>
      <% if(isAuthenticated){%>
      <li
        class="<%= path === '/cart' ? ['nav-item','active'].join(' ') : 'nav-item' %>"
      >
        <a class="nav-link" href="/cart">Cart</a>
      </li>
      <li
        class="<%= path === '/orders' ? ['nav-item','active'].join(' ') : 'nav-item' %>"
      >
        <a class="nav-link" href="/orders">Orders</a>
      </li>

      <li
        class="<%= path === '/admin/add-product' ? ['nav-item','active'].join(' ') : 'nav-item' %>"
      >
        <a class="nav-link" href="/admin/add-product">Add products</a>
      </li>
      <li
        class="<%= path === '/admin/products' ? ['nav-item','active'].join(' ') : 'nav-item' %>"
      >
        <a class="nav-link" href="/admin/products">All Products</a>
      </li>
      <li>
        <form action="/logout" method="POST">
          <input type="hidden" name="_csrf" value="<%= token%>" />
          <button type="submit">Logout</button>
        </form>
      </li>
      <% }else{%>
      <li
        class="<%= path === '/login' ? ['nav-item','active'].join(' ') : 'nav-item' %>"
      >
        <a class="nav-link" href="/login">Login</a>
      </li>
      <li
        class="<%= path === '/signup' ? ['nav-item','active'].join(' ') : 'nav-item' %>"
      >
        <a class="nav-link" href="/signup">Signup</a>
      </li>
      <% }%>
    </ul>
  </div>
</nav>
